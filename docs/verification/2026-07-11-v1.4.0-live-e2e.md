# v1.4.0 Live E2E Verification — 2026-07-11

**Versions:** codex-cli 0.144.1 (gpt-5.6-sol @ xhigh) · agy 1.1.1 · gemini-cli 0.42.0 · macOS (BSD userland, no gtimeout/timeout — bash watchdog path exercised)
**Working dir under review:** the Lisah repo (~multi-GB tree with large gitignored dirs), COUNCIL_SNAPSHOT_EXCLUDES set to the Lisah production list.

| Round | Result |
|---|---|
| Small full council (Codex + agy) | exit 0 in **36s**; both response files written; banner correct (model/effort/backend/watchdog); `~/.gemini/antigravity-cli/settings.json` untouched (no maxSessionTurns key) |
| Large prompt, Codex-only (**335KB** via stdin) | exit 0 in **21s**, clean VERDICT — old ARG_MAX ceiling and multi-minute hangs gone; full-tree snapshot completed inside the round |
| gemini-cli backend, no API key | fails honestly in **4s** with `IneligibleTierError` (oauth-personal shut off 2026-06-18) — auto mode correctly resolves to agy without a key |
| agy task-focus | **derailed 4/4 probe runs** (documented its own `--add-dir`/`--print-timeout` flags; pulled cross-session scratch incl. unrelated repo content; one unsandboxed probe wrote+ran a test script — landed only in agy's own `~/.gemini/.../scratch/`, no repo writes). NOT a plumbing issue — reproduced across three invocation styles. Mitigated by the universal verdict-gate (commit e387309): derails now surface as `[COUNCIL-ADVISOR-FAILURE] … derailed` with forensics in `gemini_derailed.txt`, aggregate exit 1, degraded-council messaging. |

**Launch-context matrix (harness = Claude Code Bash tool):** foreground + 87KB stdin ✓ · background + tiny ✓ · background + 87KB ✗ (hangs at ~0 CPU, pty wrap does not help) · nohup-detached + 87KB ✓. SKILL.md §3a mandates the nohup-detached launch accordingly.

**Recommendation recorded:** agy 1.1.1 `--print` is unreliable at task focus regardless of invocation form. For a dependable second advisor, export a paid `GEMINI_API_KEY`/`GOOGLE_API_KEY` (AI Studio) so auto mode selects the gemini-cli backend (single-shot, structurally derail-proof). agy remains the keyless fallback, verdict-gated.

**agy model-pin probe (plan step 3):** deferred — pinning a Pro tier changes nothing about the derail class, and `agy models` ids can be probed when/if agy is retained as a primary backend. Tracked in the ship summary.
